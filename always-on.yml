## this file is for service that are always on and independant from  the web services 
fs:  
    cpu_shares: 256
    privileged: true
    net: host
    #volumes:
    #    - ../freeswitch/master:/usr/local/freeswitch/conf
    #    - ../freeswitch/master/fs_cli.conf:/etc/fs_cli.conf
    #    - ../freeswitch/master/odbc.ini:/etc/odbc.ini
    #    - ../freeswitch/master/storage:/usr/local/freeswitch/storage
    extends:
        file: common.yml
        service: common
    build: ../../docker/vipconsult/fs/ 

ftp:
    net: "host"
    volumes:
        - ../../:/home
        - ../../vipconsult/proftpd/ftpd.passwd:/etc/proftpd/ftpd.passwd
    extends:
        file: common.yml
        service: common   
    build: ../../docker/vipconsult/proftpd/

simplehelp:
    net: "host"
    extends:
        file: common.yml
        service: common
    #ports: 
    #    - 139.162.199.192:80:800
    #    - 139.162.199.192:443:4430
    volumes:
        - ../../vipconsult/simplehelp:/home/simplehelp
    build: ../../docker/vipconsult/simplehelp/

logger:
    volumes:
        - /var/run/docker.sock:/var/run/docker.sock:ro
    extends:
        file: common.yml
        service: common
    image: sematext/spm-agent-docker
